# =============================================================================
# Trustful Agents — Secrets Configuration
# =============================================================================
#
# Copy this file to secrets.env and fill in the values.
# NEVER commit secrets.env to version control!
#
# These secrets are loaded alongside the generated env files:
#   - governance-api reads from .env.local
#   - Dashboards use these only for server-side operations via the API
# =============================================================================

# ---------------------------------------------------------------------------
# Database
# ---------------------------------------------------------------------------
# DB name and port come from the network config (config/networks/*.json)
# Only credentials go here.
DATABASE_USER=postgres
DATABASE_PASSWORD=change_me_in_production
DATABASE_HOST=localhost

# ---------------------------------------------------------------------------
# Pinata (IPFS)
# ---------------------------------------------------------------------------
# Used SERVER-SIDE ONLY via the governance-api IPFS proxy endpoint.
# NEVER expose these to frontend applications.
PINATA_API_KEY=
PINATA_SECRET_KEY=

# ---------------------------------------------------------------------------
# AWS SES (Email Notifications)
# ---------------------------------------------------------------------------
AWS_REGION=eu-central-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
SES_FROM_EMAIL=noreply@trustful-agents.ai

# ---------------------------------------------------------------------------
# Contract Deployment
# ---------------------------------------------------------------------------
# Only needed on build/deploy server, not on production app servers.
DEPLOYER_PRIVATE_KEY=
BASESCAN_API_KEY=

# ---------------------------------------------------------------------------
# Authentication
# ---------------------------------------------------------------------------
# JWT secret for governance-api SIWE auth
JWT_SECRET=change_me_to_a_random_string

# ---------------------------------------------------------------------------
# Application
# ---------------------------------------------------------------------------
# Override CORS if needed (default is generated from network config)
# CORS_ORIGIN=https://governance.trustful-agents.ai,https://api.trustful-agents.ai

# Server domain for SIWE authentication
DOMAIN=governance.trustful-agents.ai
