# API URL for production
VITE_API_URL=https://api.trustful-agents.ai:3001

# WalletConnect Project ID (get one at https://cloud.walletconnect.com/)
VITE_WALLETCONNECT_PROJECT_ID=your-project-id

# Contract Addresses on Base Sepolia (84532)
# These are required for claiming/voting functionality
VITE_CLAIMS_MANAGER_ADDRESS=0x...  # Update after ClaimsManager deployment
VITE_RULING_EXECUTOR_ADDRESS=0x... # Update after RulingExecutor deployment

# Server config (for server.js production mode)
PORT=3002
API_URL=http://localhost:3001
